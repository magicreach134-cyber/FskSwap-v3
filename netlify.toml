[build]
  command = "CI= npm install --legacy-peer-deps && npm run build"
  publish = ".next"
  functions = ".netlify/functions-internal"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/__netlify-server-handler"
  status = 200
  force = true
