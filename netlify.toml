[build]
  command = "CI= npm install --force && npm run build"
  publish = ".next"
  functions = "functions"

[dev]
  command = "npm run dev"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/:splat"
  status = 200
  force = true
